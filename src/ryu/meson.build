# Add the Ryu library as part of GUL

libgul_src += files([
    'f2s.c',
    'd2s.c',
])

inc += include_directories('.')

# Needed for tests
ryu_additionals = files([
    'd2fixed.c',
    'generic_128.c',
])
