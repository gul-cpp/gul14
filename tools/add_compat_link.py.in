#!/usr/bin/env python3

import os

prefix = os.environ['MESON_INSTALL_DESTDIR_PREFIX']

src = 'lib@SONAME@.so'
dst = os.path.join(prefix, '@LIBDIR@', 'libgul.so')

try:
    os.remove(dst)
except:
    pass

try:
    os.symlink(src, dst)
except:
    write('Could not create compatibility link')

